// 【子】services/api/tsconfig.json
{
  "extends": "../../tsconfig.json", // 親の設定を引き継ぐ
  "compilerOptions": {
    "outDir": "./dist",             // ビルド結果はこのフォルダ内のdistに出力
    "rootDir": "../../"             // ソースコードのルートは2階層上（プロジェクトルート）
  },
  "include": ["src/**/*.ts"],       // このフォルダ内のsrc以下をコンパイル対象にする
  "exclude": ["node_modules"] // 不要ファイルは除外
}